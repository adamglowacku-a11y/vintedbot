NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Required for hidden admin panel user list and access control.
# Get it from Supabase Project Settings -> API -> service_role.
# Never expose this as NEXT_PUBLIC and never paste it into client code.
SUPABASE_SERVICE_ROLE_KEY=
# Local dev: http://localhost:3000
# Production: https://vintly.live
NEXT_PUBLIC_APP_URL=http://localhost:3000
